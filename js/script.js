// selectors
// gender selector
let genderSelector = document.querySelector("#gender-selector"),
  // button selector
  generatorBtn = document.querySelector("#generate-btn"),
  // container selector
  container = document.querySelector(".container"),
  // body selector
  body = document.querySelector("body");
